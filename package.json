{
  "name": "backpacksweeper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "드래곤스위퍼 추리 + 백팩히어로 인벤토리를 접목한 연역형 격자 로그라이크 (Phaser 4)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "phaser": "^4.1.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.8"
  }
}
